import { userInfoType } from '../types/requsetDataType';

export const ASauthToken = '@token';

export const ASuserInfoMap: { keyName: string; value: Partial<userInfoType> } = {
  keyName: '@userInfoType',
  value: {},
};
